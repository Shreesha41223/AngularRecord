import { First2CharPipe } from './first2-char.pipe';

describe('First2CharPipe', () => {
  it('create an instance', () => {
    const pipe = new First2CharPipe();
    expect(pipe).toBeTruthy();
  });
});
